n_repeat: 3

train_params:
  split_ratio: 0.5
  lambda_mn_inits: [0, -10]
  lambda_n_inits: [0, -10]
  xi: [0, -10]

  lambda_x: null
  n_chi: 500
  Chi_lim: [-0.5, 0.5]
  label_cutoff: 1.0
  reg_param: 0.
  batch_size: 64
  lr: 0.1
  num_epochs: 3

data:
  data_name: "demand_design"  # {np, linear, demand}